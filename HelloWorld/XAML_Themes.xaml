<Page
    x:Class="HelloWorld.XAML_Themes"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HelloWorld"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <SolidColorBrush x:Key="AccentBrush" Color="{ThemeResource SystemAccentColor}"/>
    </Page.Resources>


    <StackPanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Rectangle Width="100" Height="100" Fill="{ThemeResource SystemAccentColor}"/>
        <Rectangle Width="50" Height="50" Fill="{ThemeResource SystemColorWindowColor}"/>
        <!--Setting specific font to TextBlock-->
        <TextBlock Text="My App Title" Style="{StaticResource HeaderTextBlockStyle}"/>
        <TextBlock x:Name="textBlock" TextWrapping="Wrap" Text="My App Title" Style="{StaticResource TitleTextBlockStyle}"/>
    </StackPanel>
</Page>
